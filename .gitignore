# Artefactos de microVM: pesados y reproducibles con scripts/30-fetch-artifacts.sh
*.squashfs
*.ext4
vmlinux*
bzImage*
initramfs*

# Artefactos preparados para go:embed (`make assets`). Son copias de lo que ya
# producen los scripts: ~20 MB que no tienen por qué vivir en la historia de git.
/internal/assets/blobs/

# Snapshots (el fichero de memoria pesa lo que la RAM asignada)
snap.file
mem.file
*.snap

# Sockets y config generada
*.sock
bench-*.json

# Sistema
.DS_Store

# Binarios compilados
/kling
/kling-bridge
/kling-bridge-local
/kling-linux-amd64

# Informes generados
*.html

# Restos de herramientas ajenas
.utm-workspace/

# Grafo de conocimiento (graphify): se regenera con /graphify
/graphify-out/
